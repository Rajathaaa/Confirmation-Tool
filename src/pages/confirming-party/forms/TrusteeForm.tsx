import InventoryForm from "./InventoryForm";

const TrusteeForm = ({ confirmation }: { confirmation: any }) => {
  return <InventoryForm confirmation={confirmation} />;
};

export default TrusteeForm;